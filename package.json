{
  "name": "esbehavior-monorepo",
  "version": "1.0.0",
  "description": "esbehavior is a framework for producing executable documentation.",
  "main": "index.js",
  "scripts": {
    "test": "lerna run test --scope=esbehavior --scope=esbehavior-reporter"
  },
  "author": "Brian Watkins",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
